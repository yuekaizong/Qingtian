server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
        config:
          server:
            git:
              uri: https://github.com/yuekaizong/Qingtian/
              searchPaths: CloudConfigServer
#                 username: yuekaizong
#                 password: xxx
          label: master
